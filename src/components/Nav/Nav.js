import React from 'react';

const Nav = (props) => {
    return (
        <div>
            <button>Check</button>
            <button>New Game</button>
            <button>Solve</button>
            <button>Restart</button>
        </div>
    )
}

export default Nav